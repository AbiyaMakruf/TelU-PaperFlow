<x-layouts.app title="Notifikasi" heading="Notifikasi"><div class="flex justify-between gap-4"><div><p class="eyebrow">Inbox</p><h1 class="page-title">Notifikasi</h1></div><form method="POST" action="{{ route('notifications.read-all') }}">@csrf<button class="btn btn-secondary">Tandai semua dibaca</button></form></div><div class="mt-6 space-y-3">@forelse($notifications as $item)<a href="{{ route('notifications.read',$item->id) }}" class="card block p-5 {{ $item->read_at ? 'opacity-70' : 'border-orange/30' }}"><div class="flex justify-between"><strong class="text-navy">{{ $item->data['title'] ?? 'Notifikasi' }}</strong><span class="text-xs text-muted">{{ $item->created_at->diffForHumans() }}</span></div><p class="mt-2 text-sm text-muted">{{ $item->data['message'] ?? '' }}</p></a>@empty<div class="card p-10 text-center text-muted">Belum ada notifikasi.</div>@endforelse</div><div class="mt-5">{{ $notifications->links() }}</div></x-layouts.app>
