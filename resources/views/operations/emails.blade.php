<x-layouts.app title="Monitoring email" heading="Monitoring email">
    <p class="eyebrow">Communication queue</p><h1 class="page-title">Monitoring email</h1>
    <p class="page-subtitle">Admin melihat email conference yang dikelolanya. Editorial hanya melihat email yang dikirimkan sendiri.</p>
    <div class="mt-6 grid grid-cols-2 gap-3 lg:grid-cols-4">@foreach(['queued'=>'Queue','sending'=>'Sending','failed'=>'Failed','sent'=>'Sent'] as $key=>$label)<a href="{{ route('emails.index',['status'=>$key]) }}" class="card p-4"><p class="text-xs font-bold uppercase text-muted">{{ $label }}</p><p class="mt-2 text-2xl font-black {{ $key==='failed'?'text-danger':'text-navy' }}">{{ $stats[$key] }}</p></a>@endforeach</div>
    <div class="card mt-6 overflow-hidden"><div class="overflow-x-auto"><table class="data-table"><thead><tr><th>Waktu</th><th>Conference / Paper</th><th>Pengirim</th><th>Penerima</th><th>Subject</th><th>Status</th><th></th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->created_at->format('d M Y H:i') }}</td><td><p class="font-bold">{{ $log->conference?->name ?? '-' }}</p><p class="text-xs text-muted">{{ $log->submission?->paper_id ?? $log->submission?->paper_code ?? 'Test email' }}</p></td><td>{{ $log->sender?->name ?? 'Sistem' }}</td><td><p>{{ $log->recipient }}</p>@if($log->cc)<p class="max-w-xs break-words text-xs text-muted">CC: {{ implode(', ',$log->cc) }}</p>@endif</td><td><p class="max-w-sm font-semibold">{{ $log->subject }}</p>@if($log->error)<p class="mt-1 max-w-sm text-xs text-danger">{{ Str::limit($log->error,180) }}</p>@endif</td><td><span class="badge {{ $log->status==='sent'?'badge-success':($log->status==='failed'?'badge-danger':'badge-warning') }}">{{ $log->status }}</span></td><td>@if($log->status==='failed' && $log->body)<form method="POST" action="{{ route('emails.resend',$log) }}">@csrf<button class="btn btn-secondary px-3 py-2 text-xs">Re-send</button></form>@endif</td></tr>@empty<tr><td colspan="7" class="py-10 text-center text-muted">Belum ada email.</td></tr>@endforelse</tbody></table></div><div class="p-5">{{ $logs->links() }}</div></div>
</x-layouts.app>
