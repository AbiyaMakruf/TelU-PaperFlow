<x-layouts.app title="Monitoring" heading="Monitoring"><p class="eyebrow">Operations</p><h1 class="page-title">Monitoring aplikasi</h1><div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="card p-6"><h2 class="font-black text-navy">Failed jobs</h2><div class="mt-4 space-y-3">@forelse($failedJobs as $job)<div class="rounded-xl bg-warm p-4"><p class="text-xs text-muted">{{ $job->failed_at }}</p><p class="mt-1 text-sm">{{ Str::limit($job->exception,180) }}</p><form class="mt-3" method="POST" action="{{ route('admin.monitoring.retry',$job->uuid) }}">@csrf<button class="btn btn-secondary">Retry</button></form></div>@empty<p class="text-sm text-muted">Tidak ada failed job.</p>@endforelse</div>{{ $failedJobs->links() }}</section><section class="card p-6"><h2 class="font-black text-navy">Error terbaru</h2><div class="mt-4 space-y-3">@forelse($errors as $error)<pre class="whitespace-pre-wrap rounded-xl bg-danger/5 p-3 text-xs text-danger">{{ $error }}</pre>@empty<p class="text-sm text-muted">Tidak ada error tercatat.</p>@endforelse</div></section></div></x-layouts.app>
